// APP GRAL =====================================
* Mover todas las dependencias a la carpeta /lib

// FLOODER ======================================

// FUZZER =======================================
XXXXXXXXXX Agregar opción para el modo de fuzzing. Ahora sólo s_get("INVITE_COMMON").
XXXXXXXXXX Agregar opción (tipo -l de voiper) para mostrar los modos de fuzzing.
* Crash detection

// SPOOFER ======================================
